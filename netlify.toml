[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so deep links and refreshes never 404
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
